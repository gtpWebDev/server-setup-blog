export const COMPANY_NAME = "Company name";

// NOTE: true protection is on the backend, this just determines whether registration is visible or not
// it can be manipulated by malicious users.
export const SHOW_REGISTRATION = false;

// responsive navbar heights
export const NAVBAR_HEIGHT_XS = "50px";
export const NAVBAR_HEIGHT_MD = "64px";

export const NAV_LINKS = [{ text: "Blog", path: "/blog" }];

const twitterTag = "";
const telegramTag = "";

export const LINKS = {
  twitter: `https://x.com/${twitterTag}`,
  telegram: `https://t.me/${telegramTag}`,
};
